ANBU / MELERI MVKM TRUST - LOGIN STABLE VERSION

இந்த version-ல் login verification மற்றும் Vercel Blob allow-list தொடர்பான முக்கிய bug சரி செய்யப்பட்டுள்ளது.

முக்கிய மாற்றம்:
- Vercel Blob get() response-ஐ statusCode வைத்து தவறாக reject செய்த பழைய code நீக்கப்பட்டது.
- Private Blob read: { stream, blob } முறையில் சரியாகப் படிக்கிறது.
- Admin email முதலில் Blob list இல்லாவிட்டாலும் login செய்ய முடியும்.
- Admin மட்டும் authorized email-களை add/remove செய்ய முடியும்.
- Google ID token expiry / audience / verified email checks கடுமையாக்கப்பட்டுள்ளன.
- Login API responses cache ஆகாது.
- பழைய session refresh செய்தாலும் server-ல் மீண்டும் verify செய்யப்படும்.
- Service worker cache version v16 ஆக மாற்றப்பட்டுள்ளது.

Vercel:
1. இந்த ZIP-ஐ GitHub repository-ல் upload/update செய்யவும்.
2. அதே Vercel project-ல் deploy ஆகும்.
3. Vercel Storage-ல் Private Blob store connect செய்திருக்க வேண்டும். Vercel புதிய Blob store-ஐ project-க்கு connect செய்தால் Blob authentication environment configuration வழங்கப்படும்.
4. ADMIN_EMAIL இல்லாவிட்டால் default: m​eleriskyyoga262@gmail.com (spaces இல்லை) என்ற admin address பயன்படுத்தப்படும்.
5. GOOGLE_CLIENT_ID இல்லாவிட்டால் code-ல் உள்ள existing Google Client ID பயன்படுத்தப்படும்.
6. Blob store இல்லாத சூழலில் static fallback தேவைப்பட்டால் Vercel Environment Variables-ல் ALLOWED_EMAILS = comma/newline separated allowed emails அமைக்கலாம். ஆனால் Admin add/remove persistence-க்கு Blob store பயன்படுத்துவது சிறந்தது.

குறிப்பு:
Google OAuth Console-ல் இந்த Vercel deployment domain Authorized JavaScript origin / redirect configuration-ல் சரியாக இருக்க வேண்டும். இது code-ல் இருந்து தானாக மாற்றப்படாது.
